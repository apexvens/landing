"use client";

/* Native scroll — Lenis removed for smoother perf on low-end devices */
export default function SmoothScroll({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
